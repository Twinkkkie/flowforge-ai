# FlowForge AI

Senior-grade portfolio project: a multi-tenant workflow automation platform with background execution, durable workflow versions, webhooks, AI/HTTP/condition/transform nodes, retries, idempotency, RBAC, encrypted secrets, audit logs and observability.

The repository is being built as a production-style backend system rather than a CRUD demo.
